# Pull Request: Brain MVP - 全通路訊息管理平台

## 🎯 目標

建立 Hour Jungle 的 AI 輔助客服系統，讓老闆回訊息的時間從 5-10 分鐘/則降低到 1-2 分鐘/則。

## 📋 功能範圍

### 後端 (FastAPI)

1. **Webhook 處理**
   - LINE@ Messaging API Webhook
   - 驗證簽名
   - 接收文字訊息、圖片訊息

2. **訊息管理 API**
   - CRUD 訊息
   - 取得待處理列表
   - 發送回覆
   - 手動新增訊息（for 其他管道複製貼上）

3. **AI 草稿生成**
   - 使用 Claude API (claude-3-haiku)
   - 意圖分類（詢價/預約/客訴/閒聊）
   - 產生回覆草稿
   - 附帶策略說明

4. **學習記錄**
   - 記錄原始草稿 vs 最終發送內容
   - Diff 比對
   - AI 分析修改原因
   - 統計修改率

5. **資料庫**
   - SQLite（開發環境）
   - Messages 表：訊息
   - Drafts 表：AI 草稿
   - Responses 表：回覆記錄（含學習資料）

### 前端 (React + Tailwind)

1. **Dashboard 頁面**
   - 統計卡片（待處理數、今日已處理、AI 採用率）
   - 待處理訊息列表
   - 訊息詳情 + AI 草稿
   - 編輯區 + 發送按鈕

2. **訊息卡片**
   - 來源圖示（LINE/Email/電話）
   - 客戶名稱 + 時間
   - 原始訊息內容
   - AI 策略建議
   - 草稿編輯器

3. **側邊欄**
   - 手動新增訊息（複製貼上用）
   - 最近修改記錄

4. **即時更新**
   - WebSocket 或 Polling
   - 新訊息通知

## 🏗️ 技術細節

### 後端依賴
```
fastapi>=0.104.0
uvicorn>=0.24.0
line-bot-sdk>=3.5.0
anthropic>=0.7.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
pydantic>=2.5.0
```

### 前端依賴
```
react@18
react-dom@18
react-router-dom@6
tailwindcss@3
@heroicons/react
axios
date-fns
```

### Port 配置
- 後端：8787
- 前端開發：5173
- ngrok：暴露 8787

## ✅ 驗收標準

1. [ ] LINE@ 發訊息，Dashboard 能收到
2. [ ] AI 草稿在 3 秒內產生
3. [ ] 可以編輯草稿並發送
4. [ ] 發送後 LINE 收到回覆
5. [ ] 修改記錄有保存
6. [ ] 手動新增訊息功能正常

## 📝 測試方式

```bash
# 1. 啟動後端
cd backend && uvicorn main:app --reload --port 8787

# 2. 啟動前端
cd frontend && npm run dev

# 3. ngrok 暴露
ngrok http 8787

# 4. LINE Console 設定 Webhook
# https://xxxx.ngrok.io/webhook/line

# 5. 用 LINE 發訊息測試
```

## 🚀 部署

```bash
# GCP VM
docker-compose up -d

# 或直接跑
./scripts/deploy.sh
```

## 📌 注意事項

- 不要自動回覆，一定要人工審核
- 學習記錄是核心功能，不能省略
- Port 用 8787，不要用 8000（本機有其他專案）
- 先用 SQLite，之後再遷 PostgreSQL
